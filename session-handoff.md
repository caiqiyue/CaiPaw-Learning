# Session Handoff

> **一次性草稿**：跨 session 交接用。写满一份就归档 / 覆盖。
>
> **本份写于**：2026-09-10 —— **前期准备 100% 完成，尚未开工**。
> **目标**：让下一个会话（人或 AI）能在 **3 分钟**内接上班。

---

## 一、现在在哪

| | |
|---|---|
| **阶段** | 阶段 0 结束 / 阶段 A 起点 |
| **当前模块** | `M01` 模型与供应商层 —— ⬜ **未开始** |
| **当前 feature** | `a01` 调通第一次 LLM 调用 —— `pending` |
| **内层进度** | `docs/进度/M01-*.md` **尚未创建**（开工 M01 那一刻才复制模板） |
| **仓库** | `main` 已推 GitHub，18 个文件，无敏感信息 |
| **代码** | **一行都没有** —— 这是对的，不是遗漏 |

---

## 二、接上班只需要读三份

| 顺序 | 文件 | 读什么 | 预计 |
|---|---|---|---|
| 1 | `docs/核心宗旨.md` §2 + §9 | 「一步」的定义、这个项目为什么存在 | 1 分钟 |
| 2 | `docs/任务清单-粗粒度.md` 的 **M01** 条目 | 这步做什么、产出、验收、**留给重构时决定的事** | 1 分钟 |
| 3 | `docs/harness机制_CaiPaw-Learning.md` §4.1 + §4.2 + §5.2 | 硬约束 **L1~L7**（**L7 是关键**）、**DoD 五条（§4.2）**、学习验证 | 1 分钟 |

`AGENTS.md` 是**常读**的（会话开头扫一遍 §1 硬约束），不算在 3 分钟里。

---

## 三、环境（已就绪，不用重装）

```bash
conda activate caipaw-agent          # Python 3.11 + 依赖已装
cd <仓库根>
bash init.sh                         # 应打印「还没有任何子系统目录，跳过验证」→ 退出 0
```

- **`.env`** 已配好 API key，**不入库**；模板是 `.env.example`
- **conda env**：`caipaw-agent`（`D:\anconda\envs\caipaw-agent`），独立环境，
  不与 `gpt-learning` / `langchain_learning` 混用

**装好时的实际版本**（2026-09-10 实测，`pip install -r requirements-dev.txt`）：

```text
Python                3.11.16
langchain-core        1.6.2      ← ⚠️ 注意是 1.x，不是 0.x
langchain-openai      1.6.2
langgraph             1.2.11
openai                3.11.0     ← ⚠️ 注意是 3.x
pydantic              2.13.5
ruff 0.16.6 / mypy 2.3.1 / pytest 9.1.1
```

> ⚠️ **M01 开工前必看**：`requirements.txt` 里写的是**下限**（`>=0.3` 之类），
> 实际解析到的是 **LangChain 1.x / OpenAI 3.x** —— 这两个都是**大版本**，
> API 与网上大量 0.x / 1.x 时代的示例**不一样**。
>
> **别照抄旧教程**。写代码前用 `python -c "import ...; help(...)"` 或读已装包的
> 实际签名确认。这正是 L5（看不懂的代码不进仓库）会拦下的东西。

已验证可用：`ChatPromptTemplate` / `tool` / `StateGraph` / `ChatOpenAI` 都能 import。

---

## 四、下一步具体做什么

> ⚠️ **顺序不能反：先读源码 → 再拆步骤 → 最后写代码。**
> 直接开始拆 = 做出来的是**你自己想象的 Agent**，不是 QwenPaw 的。
> 详见 `AGENTS.md` §3.1.1，以及 harness 规格 §4.1 的 **L7**。

### 1️⃣ ★ L7 闸门：精读源码（**拆步骤之前**）

**读这三个，10~30 分钟**，目标是搞清**它的机制怎么设计的**：

```text
qwenpaw-project/src/qwenpaw/agents/model_factory.py        (1407 行)
    ⚠️ 只看出口函数 create_model_and_formatter 在做什么 —— 不要读完整个文件
    （该函数在 model_factory.py:1225）

qwenpaw-project/src/qwenpaw/agents/routing_chat_model.py   (129 行)
    —— 「模型路由」是什么

qwenpaw-project/src/qwenpaw/providers/provider.py          (671 行)
qwenpaw-project/src/qwenpaw/providers/provider_manager.py  (2540 行)
    —— 看「供应商」这层抽象存在的理由（不用读全，看类结构和出口）
```

**读完要能回答四问**，每条都引用**具体函数名 / 类名**：

1. **核心抽象**是什么？（把「模型」抽象成了哪几层，各管什么）
2. **数据怎么流动**？（一次调用从入口到出口经过哪几个环节）
3. **关键决策点**在哪？（它做了哪几个「换我写肯定写成另一种」的选择）
4. **边界怎么分**？（什么放在这层，什么故意放到别处）

> ⚠️ 写「它做了个模型管理」这种空话 = **没读懂，回去重读**。

### 2️⃣ 拆 M01 的内层步骤

复制 `docs/进度/_模板-细粒度步骤.md` → `docs/进度/M01-模型与供应商层.md`，
**先把 ① 的理解填进 §2「源码精读」**，再拆成 **10~14 个小步**
（`docs/任务清单-粗粒度.md` 对 M01 的预估区间）。

**步骤表里有一列必须填：「贴着 QwenPaw 的什么」。**
填不出来 = 那一步是凭空想的 → 删掉重来。
（纯工程脚手架写「——（无对应）」，但**必须显式写**，不能空着。）

> ⚠️ **只拆 M01**。不要顺手把 M02、M03 也拆了 —— 那是「提前动手」的变体（harness §2.5.2）。

### 3️⃣ L6 闸门 + 只做第一个小步

写代码前**再读一次实现细节**（L6），然后只做第一个小步。

预计是：「建 `agent-service/` Python 项目骨架 + 一次成功的 LLM 调用」，
**50~200 行**，做完能立刻在终端看到模型返回的一段文本。

---

## 五、卡在哪

**没卡。** 前期准备全部完成并实测通过（见 `docs/harness机制_CaiPaw-Learning.md` §0.4）。

---

## 六、⚠️ 新会话开头别忘的两件事

### 1. 启用提交防线（**本地配置，不随仓库走**）

```bash
git config core.hooksPath .githooks
```

换机器 / 重新克隆后**必须重设**，否则 `.githooks/pre-commit` 不生效 ——
那道防线负责拦「密钥和服务器标识被提交」。

### 2. 不要往仓库里写服务器标识信息

主机别名 / IP / 绝对路径 / GPU 型号 / 端口 / 用户名 —— 一律**不写进入库文件**。
真值只允许存在于 `docs/服务器运维手册.md`（**不入库**）。硬约束 **E11**。

---

## 七、需要你拍板的事

| # | 事项 | 为什么现在最该看 |
|---|---|---|
| 1 | `docs/任务清单-粗粒度.md` 的 **24 个模块、粒度和顺序** | 它决定后面几个月的推进节奏，**改它的成本现在最低** —— 开工后再改，返工的是已写好的代码和笔记 |
| 2 | 目录名 `agent-service` / `business-service` / `web` / `deploy` | 建目录时可改，现在改成本为 0（建议沿用） |

---

## 八、完整背景（想深挖时看）

见 `claude-progress.md` 的 `## Session Log`：七轮改动全记录，包括——

- **踩过的坑**：① 一个「护栏永远放行」的严重 bug（`case ... in *$'\0'*)` 写法错误）；
  ② 「部分私有」的中间地带如何埋雷；③ **护栏本身泄漏了它要保护的东西**；
  ④ **用真 key 去测「key 会不会被拦」，把 key 送进了本地对象库**
- **三次安全处理**：服务器信息泄漏 10 处 → 改写历史；`docs/` 收紧为完全私有 → 改写历史；
  护栏黑名单外移 + 清除悬空对象 → 再次改写历史
- **两个「看起来像验证、实则引入新风险」的案例** —— 都记在 harness 规格 §3.2.4

---

<!-- ============================================================
     下一份交接从这里开始写。模板：

## Session NNN — YYYY-MM-DD

**当前模块**：M0x · <模块名>（状态：进行中 / 完成 / 阻塞）
**当前 feature**：`a0x`（状态：in_progress / passing / blocked）
**内层进度**：`docs/进度/M0x-*.md`（细粒度步骤走到第 N 步）

### 做到哪了
<一两句话>

### 卡在哪（若有）
<具体现象 + 已经试过什么 + 排除了什么>

### 下一步接什么
<下个 session 第一件事，要具体到能直接动手>

### 需要人工决策的
<留给用户的开放问题>
============================================================ -->
