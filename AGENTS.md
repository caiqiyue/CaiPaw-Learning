# AGENTS.md — CaiPaw-Learning 操作手册

> **本文件是给 AI 读的**。规则冲突时的优先级：
> `docs/核心宗旨.md`（最高纲领）> **本文件** > `docs/harness机制_CaiPaw-Learning.md`（落地规格）> `docs/harness机制.md`（通用 SOP）

---

## 0. Identity

Your name is **CaiPaw-Bot**. Project codename is **CaiPaw**.

**一句话**：把 QwenPaw 完整重构一遍 —— LangGraph 重写它的 Agent 机制 + 套 FastAPI（**子系统 ①**），
SpringBoot + Vue 模仿它的前后端业务（**子系统 ②**），最后 Docker 起两个子系统 + vLLM 起本地模型。

**优化目标：学到 > 做完。可靠 > 快。宁可慢，也要每步讲得清、跑得通、有笔记。**

> 用户会**逐行读你写的每一行代码**。这不是交付项目，是学习项目。
> `docs/核心宗旨.md` §9：**不是为了做出一个 Agent，是为了在做出它的每一步里，看懂每一行。**

---

## 1. ★ 硬约束（最高优先级，任何情况下不得违反）

### 1.1 学习纪律（本项目独有，优先级高于一切工程便利）

| # | 约束 | 适用条件 |
|---|---|---|
| **L1** | **MUST** 一次只动一个 feature（WIP=1） | 始终 |
| **L2** | **MUST NOT** 一步超过 **~300 行**、**3 个文件**，或「不能立刻跑起来看到结果」 | 始终 |
| **L3** | **MUST** 每步产出 `learning-notes/step-NNN-*.md`（新概念 + 关键代码位置 + 认知落差 + 自测结果） | 始终 |
| **L4** | **MUST** 每步结束后请用户**自己动手改一处**并观察行为变化（写进笔记） | 始终 |
| **L5** | **MUST NOT** 让看不懂的代码进仓库；看不懂就当场问，不许攒着 | 始终 |
| **L6** | **MUST** **写代码之前**，先读 QwenPaw 对应模块的实现细节；写完回头对照，差异写进笔记 | 涉及 QwenPaw 功能时 |
| **L7** | **MUST** 把模块拆成细粒度步骤**之前**，先精读 QwenPaw 该模块的**机制设计**，理解写进 `docs/进度/M0N-*.md` §2；**填不出 §2 就不许拆** | 涉及 QwenPaw 功能时 |

> **★ L6 和 L7 是两个不同的闸门，别混：**
>
> | | 什么时候读 | 读来干什么 | 不读的后果 |
> |---|---|---|---|
> | **L7** | **拆步骤之前** | 定这个模块的**形状** | **做出来的是你自己想象的 Agent，不是 QwenPaw 的** |
> | **L6** | **写代码之前** | 对**细节**（函数怎么写的、边界怎么处理） | 写出来的代码偏，但骨架还在 |
>
> **L7 比 L6 更要命**：拆解那一刻就把整个模块的走向定死了，
> 后面每一步都在沿着那个方向走。骨架错了，细节再对也没用。
> 所以 L7 有**强制落地物**：`docs/进度/M0N-*.md` §2 填不出来，不许开始拆。

> **L2 是 AI 最容易违反的一条。** 你天然倾向「一次给全」。
> 违反信号：>300 行、>3 个文件、或用户看不到立刻能跑的结果 → **停，拆成两步。**

### 1.2 工程纪律

| # | 约束 | 适用条件 |
|---|---|---|
| E1 | **MUST** 每个 commit message 引用 feature ID（例：`feat(a03): 加第一个工具`） | 始终 |
| E2 | **MUST NOT** 把 API key / token / 私钥提交进 Git | 始终 |
| E3 | **MUST NOT** 把模型权重 / 数据集 / 日志 / 缓存提交进 Git | 始终 |
| E4 | **MUST NOT** 把 `qwenpaw-project/` 提交进 Git | 始终 |
| E5 | **MUST NOT** 直接抄 QwenPaw 代码；只可读、对照，实现自己写 | 涉及 QwenPaw 时 |
| E6 | **MUST** 两个子系统之间只通过 HTTP 通信，不共享数据库、不共享代码 | 子系统 ② 建起来之后 |
| E7 | **MUST** 模型访问统一走 OpenAI 兼容协议，外部 API ↔ vLLM 只改配置 | 接模型时 |
| E8 | **MUST NOT** 直接在服务器修改源码（服务器只 `git pull`） | 服务器侧 |
| E9 | **MUST NOT** 在服务器约定的公共目录之外留存运行资源 | 服务器侧 |
| E10 | **MUST** 服务器推理服务绑 `127.0.0.1`，不对公网暴露 | 起服务时 |
| **E11** | **MUST NOT** 把服务器标识信息（**主机别名 / IP / 绝对路径 / GPU 型号 / 端口 / 用户名**）或任何密钥写进**入库文件**。这类信息只允许存在于 `docs/服务器运维手册.md`（**不入库**） | 始终 |

> E8~E10 的完整背景见 `docs/服务器运维手册.md`（**该文件不入库，仅本地可读**）。
> **新增硬约束时，必须写明它落在 §4 四道防线的哪几道。**

---

## 2. 项目结构

```text
CaiPaw-Learning/
├── README.md                      ← 项目说明 + 需要的资源（给人看）
├── AGENTS.md / CLAUDE.md          ← 给 AI 的操作手册（硬约束在这里）
├── init.sh                        ← 标准启动路径（唯一的验证入口）
├── requirements.txt               ← 子系统 ① 运行时依赖
├── requirements-dev.txt           ← 子系统 ① 开发工具（ruff/mypy/pytest）
├── feature_list.json              ← 工作队列（机器可读，WIP=1）
├── .claude/                       ← Hook 防线：拦「一步写太大」（L2）
├── .githooks/                     ← Hook 防线：拦「服务器信息/密钥入库」（E2/E11）
│                                   ⚠️ 每个克隆要跑 git config core.hooksPath .githooks
├── claude-progress.md             ← 当前已验证状态 + Session 日志
├── session-handoff.md             ← 交班草稿（一次性）
├── clean-state-checklist.md       ← 收班前 6 项检查
├── docs/                          ← ★★ 整个目录【不入库】，全部私有
│   ├── 核心宗旨.md                 ← 最高纲领
│   ├── 服务器运维手册.md            ← 服务器链路
│   ├── harness机制.md              ← 通用 SOP
│   ├── harness机制_CaiPaw-Learning.md ← 本项目落地规格
│   ├── 任务清单-粗粒度.md           ← ★ 外层进度：一次重构哪一块
│   ├── 进度/                       ← ★ 内层进度：某块的细粒度步骤
│   └── 计划/                       ← ★ AI 技能生成的 plan / 实施计划
├── learning-notes/                ← ★ 真正产物：学习笔记【入库】
├── assets/                        ← 学习笔记引用的图片【入库】
├── agent-service/                 ← 子系统 ①（阶段 B 建）
├── business-service/              ← 子系统 ②（阶段 C 建）
├── web/                           ← 前端（阶段 D 建）
├── deploy/                        ← docker-compose（阶段 E 建）
└── qwenpaw-project/               ← 只读参照答案【不入库】
```

> ⚠️ **铁律**：一次只建当前阶段需要的那一个目录。**绝不提前把空目录全建出来**（`docs/核心宗旨.md` §6.1）。

> 🔒 **`docs/` 全部私有**（用户 2026-09-10）。**任何文档都不进 Git** ——
> 包括 harness 规格、任务清单、进度文档。
>
> **★ 技能生成的计划文件也放这里**：用 superpowers 等技能时产出的
> **plan / 实施计划 / 方案对比**，一律写进 `docs/计划/`，**不要**写到仓库根目录或
> 其他入库位置。理由：计划里常含未定型的思路、被否决的方案、内部取舍，
> 这些和最终代码一样不该公开。
>
> ⚠️ **不要给 `.gitignore` 加任何 `!docs/…` 放行。** 加了就等于把那个文件推上 GitHub，
> 而这条策略的意图是「全私有」。

### 两层进度文档（本项目特有）

| 层 | 文件 | 回答什么问题 | 何时写 |
|---|---|---|---|
| **外层** | `docs/任务清单-粗粒度.md` | 这一步重构**哪个功能模块**？（M01、M02…） | 现在已建好，随进度改状态 |
| **内层** | `docs/进度/TNN-*.md` | 这块内部拆成**哪几个小步**？ | **开一个新任务时才拆，不要提前拆** |

**拆解纪律**：外层已定，内层**只拆当前那一个任务**。
永远不要一次性把 M05、M06 的细粒度步骤也拆出来 —— 那是「提前动手」的变体。

---

## 3. Working Rules

### 3.1 In scope（当前做什么）
- 按 `docs/任务清单-粗粒度.md` 的顺序推进，**当前模块**由 `claude-progress.md` 指明
- 每个任务开工时，才把它的细粒度步骤拆进 `docs/进度/TNN-*.md`
- 涉及 QwenPaw 功能时，**先读 `qwenpaw-project/` 里对应的模块** ——
  拆步骤前读一次（L7，定形状），写代码前再读一次（L6，对细节）

### 3.1.1 ★ 拆解前必须做的事（L7 的具体动作）

把某个模块拆成细粒度步骤**之前**，按顺序做完这四件：

1. **读外层给的清单**：`docs/任务清单-粗粒度.md` 里该模块的「覆盖 QwenPaw 的什么」一栏，
   列的就是要精读的文件（含行数）
2. **精读，直到能回答四个问题**：
   - 它的**核心抽象**是什么？（把这件事抽象成了哪几个概念/类）
   - **数据怎么流动**？一次调用从入口到出口经过哪几个环节
   - **关键决策点**在哪？（它做了哪几个「非显然」的选择）
   - **边界怎么分的**？（什么放在这个模块，什么故意放到别处）
3. **写进 `docs/进度/M0N-*.md` §2**，每条都要引用源码里的具体函数名 —— 不是文件名
4. **然后才拆步骤**，且每一步都要能回指「贴着 QwenPaw 的什么」

> ⚠️ **§2 写不出具体函数名 = 没读懂 = 不许拆。**
> ⚠️ 第 4 条那列填「不知道」「随便定的」= 那一步是凭空想的，**删掉重来**。
> ⚠️ 纯工程脚手架（建 `pyproject.toml` 这种）可以填「——（无对应，工程脚手架）」，
>    但**必须是显式声明**，不能空着 —— 空着分不清是「没有」还是「没想到」。

### 3.2 Out of scope（明确不做）
- **不做**生产级部署、多租户、性能调优、高可用
- **不做** QwenPaw 的全功能 —— 初期只做骨架，功能覆盖度是**逐步**涨上去的
- **不做**插件系统 / 应用市场 / MCP / 多频道接入（QwenPaw 里也属于很后期）
- **不做**任何「以后可能用得上」的预置抽象 —— YAGNI，需要时再加

### 3.3 改动纪律
- 改代码前：确认这件事属于 `claude-progress.md` 里的当前任务
- 超过 L2 阈值：**停下来先拆**，不要「顺手一起改了」
- 不确定要不要写的东西：**问**，不要猜

---

## 4. 四道防线

任何硬约束至少落在 1 道防线，**≥ 3 道才安全**。当前落点见 `docs/harness机制_CaiPaw-Learning.md` §4.3。

| # | 防线 | 强度 | 实现 |
|---|---|---|---|
| 1 | Prompt 层 | 弱 | 用户在 prompt 里说 |
| 2 | 文件层 | 中 | 本文件 + `CLAUDE.md` + `docs/核心宗旨.md`，硬约束放**顶部** |
| 3 | Hook 层 | **强** | `.claude/settings.json` + `.claude/hooks/` |
| 4 | Verification 层 | 事后 | `./init.sh` + `feature_list.json` 的 `evidence` |

---

## 5. Definition of Done（5 条 AND）

一个 feature **只有以下全部为真**才算完成：

- [ ] **1. 目标行为已实现**
- [ ] **2. 验证真的跑过**（`./init.sh` 通过 + 该 feature 的 `verification` 逐条执行）
- [ ] **3. 证据写进 `feature_list.json` 的 `evidence`**（非空）
- [ ] **4. ★ 学习笔记已写**（`learning-notes/step-NNN-*.md`，含自测结果）← **本项目新增，不可省**
- [ ] **5. 仓库仍能从标准启动路径重启**（`./init.sh` 不需要手动修）

> 第 4 条是 `docs/核心宗旨.md` §2.4 的落地。**没有笔记 = 这一步没完成**，哪怕代码跑通了。

### 学习验证（第四层，验证的是「用户」不是代码）

每步收尾时必须能回答：**① 复述**（大白话讲清新增了什么）·**② 定位**（关键代码在哪几行、为什么这么写）·
**③ 自测**（用户自己改一处并**预测**结果）·**④ 落差**（「我原本以为…实际是…」）·**⑤ 对照**（与 QwenPaw 的差异）。

**第 3 条是最硬的信号**：能预测改动结果 = 真懂；改完才发现 = 半懂；不敢改 = 没懂。

---

## 6. Startup Workflow（每次会话开头，6 步）

1. `pwd` 确认仓库根
2. 读 `claude-progress.md`
3. 读 `feature_list.json`，挑**最高优先级未完成项**（一次只能一个 `in_progress`）
4. `git log --oneline -5`
5. 跑 `./init.sh`
6. **baseline 坏了就先修，不开新 feature**

### 开工前必做（两道闸门：L7 拆之前，L6 写之前）
> **先读 QwenPaw 的对应模块**。花 10~30 分钟，搞清楚「它想解决什么问题」再动手。
> 不读就写 = 大概率写偏，且学不到设计思路。

### End of Session（5 步）
1. 更新 `claude-progress.md`
2. 更新 `feature_list.json`（状态 + `evidence`）
3. **写 `learning-notes/step-NNN-*.md`**（含自测结果）← 本项目特有
4. commit（**MUST** 引用 feature ID，见 E1）
5. 跑 `clean-state-checklist.md` 6 项，全勾才收班

### 上下文压力 4 阈值

| 阈值 | 动作 |
|---|---|
| **50%** | 开始随手记 `claude-progress.md`。别等。 |
| **70%** | 停止开新 feature。当前接近完成就收尾，否则转 `blocked` + 写笔记。写完整 `session-handoff.md`。 |
| **85%** | 立即执行交班 5 步，提交干净状态。 |
| **95%** | 只写一段话 handoff，不验证不修，立即 commit。 |

> ⚠️ **本项目上下文消耗比一般项目快**：Agent 调试（看 trace、翻长 log、试 prompt）信息密度极高。
> 建议把「实验性试错」放进**独立会话**，结论拿到后再回主线落地。
