# ============================================================
# CaiPaw-Learning — 子系统 ① 开发工具依赖
# ============================================================
# 装了这份，`bash init.sh` 才能在 agent-service/ 上跑通。
#
#   pip install -r requirements-dev.txt
#
# ⚠️ 版本同样是「起步约束」而非锁定版本，理由见 requirements.txt 顶部。
# ============================================================

-r requirements.txt

# ---- 静态检查（init.sh 的 [①/静态] 段要用）----
ruff>=0.6
mypy>=1.11

# ---- 测试（init.sh 的 [①/单元] 段要用）----
pytest>=8.2
pytest-asyncio>=0.23

# 注：不装 types-requests —— 本项目用 httpx，它自带类型标注，不需要额外存根。
