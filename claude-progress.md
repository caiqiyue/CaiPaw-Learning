# Claude Progress

> **两段结构**：上面的 `## Current Verified State` 是**可覆盖**的快照（永远反映此刻真实状态）；
> 下面的 `## Session Log` 是 **append-only** 的日记（只追加，不修改历史）。

---

## Current Verified State

**更新时间**：2026-09-10（Session 000 结束）
**当前阶段**：**阶段 0 完成 → 阶段 A 起点**。前期准备 100% 完成，**还没有任何可运行的代码**
**当前模块**：`M01` 模型与供应商层（见 `docs/任务清单-粗粒度.md`）—— ⬜ 尚未开工
**当前 feature**：`a01` 调通第一次 LLM 调用 —— `pending`（`feature_list.json` 里 5 行全是 pending）

> 📌 **接手请先看 `session-handoff.md`** —— 那份是按「3 分钟接上班」写的。

### 仓库里现在有什么

| 类别 | 文件 | 状态 |
|---|---|---|
| 项目说明 | `README.md` | ✅ 含**资源清单**（各软件版本 + 何时需要）+ 跨平台说明 |
| 依赖声明 | `requirements.txt` / `requirements-dev.txt` | ✅ 起步约束（非锁定版本），阶段 B 的部分注释待开 |
| **docs/ 全部** | `docs/` 整目录 | ✅ **完全私有**（远端与历史里都没有） |
| 最高纲领 | `docs/核心宗旨.md` | ✅ 定稿 |
| 服务器链路 | `docs/服务器运维手册.md` | ✅ 已回填（**服务器标识真值在这里**） |
| 通用 SOP | `docs/harness机制.md` | ✅ 已有 |
| 落地规格 | `docs/harness机制_CaiPaw-Learning.md` | ✅ v3 已落地 |
| **外层进度** | `docs/任务清单-粗粒度.md` | ✅ v2：**24 个功能模块 M01~M24** + 39 router 覆盖度矩阵 |
| **内层进度** | `docs/进度/` | ✅ README + 模板；**故意无实例**（开工时才生成） |
| **计划落点** | `docs/计划/` | ✅ README；技能生成的 plan 都放这（天然不入库） |
| 环境变量 | `.env`（真值）/ `.env.example`（入库） | ✅ API key 已配；**强加测试验证过拦得住** |
| **conda 环境** | `caipaw-agent` | ✅ Python 3.11 + 依赖已装 |
| AI 手册 | `AGENTS.md` / `CLAUDE.md` | ✅ 已建 |
| 验证入口 | `init.sh` | ✅ 检测式，空仓库跑通（exit 0） |
| 工作队列 | `feature_list.json` | ✅ 仅 `a01`~`a05` → 落在 M01 / M01 / M02 / M03 / M03 |
| 状态机 | `claude-progress.md` / `session-handoff.md` / `clean-state-checklist.md` | ✅ 已建 |
| Hook 防线 ① | `.claude/settings.json` + `hooks/check-step-size.sh` | ✅ 拦 >300 行的单次写入（7 用例实测） |
| Hook 防线 ② | `.githooks/pre-commit` | ✅ 拦服务器信息 / 密钥入库（E2/E11，5 用例实测） |
| ★ **学习产物** | `learning-notes/` | ⬜ 空（等第一步产出）【**入库**】 |
| 图片 | `assets/` | ⬜ 空（等第一张图）【**入库**】 |
| 忽略规则 | `.gitignore` | ✅ 已按**两套子系统**分组重写 |
| 子系统 ① | `agent-service/` | ⬜ **尚未创建**（故意，见铁律） |
| 子系统 ② | `business-service/` / `web/` | ⬜ **尚未创建**（故意） |
| 参照物 | `qwenpaw-project/` | ✅ 就位【**不入库**】 |

### 验证状态

```bash
./init.sh    # → ⚠️ 还没有任何子系统目录，跳过验证。（退出码 0）
```

**baseline 是否健康**：✅ 健康（没有代码，也没有坏掉的东西）

### 下一步

```bash
conda activate caipaw-agent
cd <仓库根> && bash init.sh        # 应打印「跳过验证」并退出 0
```

> ⚠️ **顺序不能反：先读源码 → 再拆步骤 → 最后写代码**（L7 / L6 两道闸门）

1. **L7 闸门**（拆步骤**之前**，别跳）：精读 `qwenpaw-project` 里的
   `agents/model_factory.py`（只看 `create_model_and_formatter`，在 L1225）、
   `agents/routing_chat_model.py`、`providers/provider.py` + `provider_manager.py`
   → 读完要能回答四问（核心抽象 / 数据流动 / 关键决策点 / 边界划分），
     **每条引用具体函数名**
2. 从 `docs/进度/_模板-细粒度步骤.md` 复制出 `docs/进度/M01-*.md`，
   **先把 ① 的理解填进 §2「源码精读」**，再拆成 10~14 小步
   —— **只拆 M01**，别提前拆后面的
3. **L6 闸门** + 只做第一个小步：写代码前再读一次实现细节，
   然后建 `agent-service/` 骨架 + 一次成功的 LLM 调用

> ⚠️ 新会话开头先跑 `git config core.hooksPath .githooks`（本地配置，不随仓库走）。

---

## Session Log

> append-only。每条按 `docs/harness机制.md` §5 的模板：做了什么 / 验证到什么 / 证据 / 下一步。

### Session 000 — 2026-09-10 · 搭 harness（无代码）

**做了什么**
- 确定技术栈与项目定位（`docs/核心宗旨.md` v3）
- 写 `.gitignore` / `.gitattributes`（含本仓库特有防线：`qwenpaw-project/`、`docs/` 选择性入库）
- 写 `docs/harness机制_CaiPaw-Learning.md`（落地规格）
- 补齐 `docs/服务器运维手册.md` §13（CaiPaw-Learning 章节）
- **本 session**：搭完整 harness —— L1 四件套 + L2 三项 + Hook 防线 + 两层进度文档
- 子智能体分析 `qwenpaw-project` → 产出 `docs/任务清单-粗粒度.md`

**验证到什么**
- `./init.sh` 在空仓库状态下跑通（打印跳过提示，退出 0）
- `git check-ignore` 逐条验证过 `docs/` 的选择性入库规则

**证据**
- `docs/harness机制_CaiPaw-Learning.md` §7.1 落地验收清单

**踩过的坑（写给未来的自己）**
1. 栈没定就先建文件 → 返工。**先定，再建。**
2. 服务器只会 `git pull` 不会 `git push` → 别在服务器上热修（E8）
3. **「重构」很容易做成「抄代码」** → E5，必须自己写
4. **任务很容易切太大** → 用 L2 的量化阈值卡住（`.claude/hooks/` 里已加防线）
5. ~~`docs/` 里混着「要入库」和「不要入库」两类文档 → 用 `docs/*` + `!` 白名单~~
   **已废弃**：白名单方案被推翻，`docs/` 改为**整目录私有**。
   教训是「部分私有」这件事本身就别扭 —— 每加一个文档就要判断一次该不该放行，
   迟早有人加错一行。**要么全公开，要么全私有，别在中间地带。**

**审计（独立子智能体，对抗式复核）**

本 session 收尾时跑了一轮独立审计，**它找出了 6 处真实缺陷，已全部修复**：

| # | 缺陷 | 修复 |
|---|---|---|
| 1 | `docs/核心宗旨.md` §8/§8.1 仍写着 harness 文件「待创建」、且把文档画在仓库根 | ✅ 已改为 `docs/` 路径 + 实际状态 |
| 1b | `docs/服务器运维手册.md` §13 **当时没改干净**（`§13.10.3` 的下一步仍写着「本地 git init + 建 L1 四件套」）—— 当时的记录写的是「已全部改」，**是过度声明** | 第八轮补修 |
| 2 | harness 规格 §4.3.1 写「3 条标记为弱」，实际标了 4 条 | 改为 4 条 |
| 3 | 任务清单 5 处 QwenPaw 路径缺目录前缀（`console.py` / `agent_status.py` 等） | 已补全 |
| 4 | 任务清单写「11 个 provider 文件」，实际 8 个 | 已改正 |
| 5 | §7.1 把「3 分钟接上班」勾成**已实测**，但没人开过新会话 | 移到「尚未验证」并说明 |
| 6 | Hook：相对路径 + 行长差一（301 行内容会算成 300 而放行）+ Windows 路径幻影计数 | 改 `${CLAUDE_PROJECT_DIR:-.}`；先删 `\\` 再数 `\n` 且 `+1`。**复测：300 行放行、301 行拦截，计数精确** |

审计同时确认了这些**没有**问题：全部 15 个文件存在且行数属实、`.gitignore` 行为经 `git add --dry-run` 实测正确、
任务清单确实是粗粒度（无实现步骤）、`docs/进度/` 确实没有提前生成实例、
任务清单引用的 ~48 个 QwenPaw 路径**全部存在且行数精确**（这是全份交付里最扎实的部分）、
`clean-state-checklist.md` 官方 6 项与通用仓库逐字一致。

**第二轮：外层任务清单返工（用户否决 v1）**

用户指出 v1 的两个问题，**根因是同一个**：我按「技术里程碑」切，而用户要的是按
「QwenPaw 的**功能模块**」切。后果是**数了 29 个却只覆盖不到一半功能面**
（skill / MCP / 工作区 / 审批 / 定时 / 用量 / 语音 / 插件全被塞进「T30+」）。
另外 v1 还把 MCP、插件市场标成「不做」，理由是「核心宗旨 §5.3 标为 E（很后期）」
—— **「很后期」不等于「不做」**，是把自己的纲领读反了。

- 重写为 **24 个功能模块 M01~M24**，按执行顺序编号；每个含 8~20 个细粒度步骤
- 新增 **§3 覆盖度矩阵**：39 个 router 逐个列归属，外加 8 顶层页面 / 6 Agent 子页 /
  4 Control 子页 / 13 Settings 子页
- 唯一不进清单的只有**生产级加固**（多租户 / 性能 / 高可用 / CI-CD）
- 教训写进 `harness机制_CaiPaw-Learning.md` **§9.4**

**第三轮：仓库结构定稿**

- `notes/` → 拆成两个目录：**`learning-notes/`**（笔记，入库）+ **`assets/`**（图片，入库）
- **`.gitignore` 按两套子系统重组**（① Python / ② Java / ② Vue / 部署 / 仓库级），
  不再混成一张防御式大表
- 全仓库 `notes/` → `learning-notes/` 批量改名（13 个文件、45 处引用）

**第四轮：跨平台 + 资源声明**

- `.gitattributes` 补 `mvnw` / `gradlew` —— **无扩展名的脚本 Git 认不出来**，
  在 Windows 上会 checkout 成 CRLF，拿到 Linux 执行报 `bad interpreter: ...^M`
- `init.sh` 硬化：探测 `python3`/`python`、`cd "$(dirname "$0")"`（可从任意路径调用）、
  `mvnw` 可执行位兜底、工具缺失给安装提示、只用 bash 3.2 兼容语法（macOS）
- 可执行位修正：`init.sh` 在 Windows 上被记成 `100644`，Linux 上 `./init.sh` 会
  Permission denied → `git update-index --chmod=+x`
- 新增 `README.md`（资源清单，带「何时需要」列，避免提前装一堆）+ `requirements.txt`
  + `requirements-dev.txt`
- **首次推送**：21 个文件，commit `3c2ccc3`

**第五轮：★ 服务器信息泄漏修复（安全）**

首次推送后复查发现**已入库文件里有 10 处服务器标识**，全部清除：

| 泄漏内容 | 位置 |
|---|---|
| `ssh -A <服务器别名>` | harness 规格 §7.3 |
| 服务器仓库的绝对路径 | harness 规格、`AGENTS.md`、`CLAUDE.md`、`README.md` |
| GPU 型号 + 分配 | `README.md` |
| 被服务的模型名 | `README.md`、harness 规格 |

- 全部换成占位符，**真值只留在 `docs/服务器运维手册.md`（不入库）**
- 新增硬约束 **E11**：服务器标识不许写进入库文件
- 新增 **`.githooks/pre-commit`** 防线（E2 + E11 从「只写在文档里」升级为系统强制）
- **踩到的坑**：第一版 hook 用 `case "$content" in *$'\0'*)` 跳二进制 ——
  bash 变量存不下 NUL，`$'\0'` 退化成空串，模式变成 `**`，
  **结果每个文件都被跳过、hook 永远放行**。一个总是说 OK 的护栏比没有更危险。
  改用 `git diff --cached --numstat` 判断
- 第二个坑：GPU 型号用 `\bV100\b` 会命中 `.gitignore` 里的 `.Spotlight-V100`
- **改写首次提交并强推**，把已泄漏内容从 Git 历史中彻底清除

**第六轮：docs/ 完全私有化 + 计划文件落点规则**

用户决定：**整个 `docs/` 都不上传**（此前只私有其中 2 份，harness 机制文档是公开的）。

- `.gitignore` §6.2 从 `docs/*` + 白名单，改为**一行 `docs/` 整目录忽略**
- 已入库的 5 份文档 `git rm --cached` 撤下，并**改写历史**（不是加一个删除提交 ——
  那样内容仍留在历史里可被翻出）
- **新增核心要求**：用 superpowers 等技能产生的 **plan / 实施计划**，一律写进
  `docs/计划/`，因此天然不入库
- 新增 `docs/计划/README.md` 说明命名与「计划 / 进度 / 笔记」三者的区别

> **教训（已写进 claude-progress 的坑列表）**：「部分私有」这个中间地带本身就是包袱 ——
> 每新增一个文档都要判断一次该不该放行，迟早有人加错一行。
> **要么全公开，要么全私有。**

**第七轮：前期准备收尾**

- **harness 文档对齐现状**：§0.4 改为「前期准备完成度」实测表；§7.0 十四步全 ✅；
  §9 待定项收敛到 2 条；§11「可以开工」；§12 差异汇总补 E11 与提交防线
- **`session-handoff.md` 重写**（八节，按「3 分钟接上班」组织）
- **conda 环境 `caipaw-agent` 建好**：Python 3.11.16 + 全部依赖（见下）

**★ 建环境时发现一件影响 M01 的事**：`requirements.txt` 写的是**下限**
（`langchain-core>=0.3` 之类），实际解析到的是 **LangChain 1.6.2 / LangGraph 1.2.11 /
openai 3.11.0** —— 都是**大版本**，API 与网上大量旧示例不同。
**M01 写代码前必须确认实际签名，别照抄教程**（这正是 L5 要拦的）。
已记进 `session-handoff.md` 的环境一节。

**第八轮：★ 首轮独立审核查出的 2 个真问题（已修）**

审核子智能体做对抗式复核，**没有空手而归** —— 两个问题都是**我上一轮的「验证」造出来的**：

| # | 问题 | 根因 | 修复 |
|---|---|---|---|
| 1 | **护栏本身泄漏了它要保护的东西** —— `.githooks/pre-commit` 是**入库文件**，却把项目专属黑名单（主机别名 / 用户名 / GPU 型号）写在了里面 | **要拦住某个词，就必须先写下它**。「自己豁免自己」≠「自己是私有的」—— 我当时只论证了前者 | 规则分两层：通用规则（凭据格式 / IP）留在 hook；**项目专属词移到 `.githooks/blocklist.local`（已 gitignore）**。hook 不存在该文件也能跑 |
| 2 | **「验证 `.env` 安全」的测试把 key 送进了 `.git/objects`** —— `git add -f .env` 即使没产生提交，也已把内容写成悬空 blob | **用真值做危险测试** —— 验证的过程本身就是风险 | `git gc --prune=now` 清除；复验**全部 git 对象里搜不到该 key**（key 未离开本机，无 push） |

**审核还查出 12 处文档不一致，全部已修**：硬约束条数 16→17、工程纪律 10→11、`E1~E10`→`E1~E11`、
`task` 字段名（实为 `module`）、`docs/` 份数 7→8、`§3.4` 过时标题、
「选择性入库」残留措辞、`session-handoff` 的「六轮」与 §4.1/§5.2 指错章节、
`CLAUDE.md` 兜底表丢失「适用条件」列、`docs/服务器运维手册.md` §13 状态过时、
以及我在第七轮里写的**一条过度声明**（声称 §13「已全部改」，实际没改干净）。

**两条方法论（已写进 harness 规格 §3.2.4 与 `CLAUDE.md`）**：

> 1. **验证方式本身要接受审查。** 这两处都不是「忘了做某件事」，
>    而是「做了一件**看起来像验证**的事，却引入了新风险」。
> 2. **不要用真实值做危险测试。** 假 key、假主机名一样能测出 hook 是否生效。

**因提交历史里仍含旧 hook 内容（那些词），本轮再次改写历史。**

**第九轮：★ 新增硬约束 L7「拆步骤前必须先读源码」**

用户提出的核心要求：**粗粒度模块拆成细粒度步骤之前，必须先读 QwenPaw 该模块的源码，
理解它的机制设计** —— 否则拆出来的步骤是「我们自己随意想出来的」，
重构出来的 Agent 机制**贴不上 QwenPaw**。

**这戳中了一个真的顺序漏洞**：原来的模板 §2「前置检查」只有四个勾选框 + 一句
「读懂了它想解决什么问题：<一两句>」，**而它就在步骤表前面**。
勾三下就能往下随便编步骤 —— 因为拆解那一刻就把模块的走向定死了，
而**没人强制你在那一刻之前读过任何东西**。

**L6 与 L7 是两个闸门**：

| | 时机 | 读来干什么 | 不读的后果 |
|---|---|---|---|
| **L7** | **拆步骤之前** | 定模块的**形状** | **做出来的是自己想象的 Agent** —— 骨架错了救不回来 |
| **L6** | **写代码之前** | 对**细节** | 写偏，但骨架还在，好救 |

**L7 的落地物（不是口号）**：
- `docs/进度/M0N-*.md` **§2「源码精读」** —— 要引用**具体函数名 / 类名**（不是文件名），
  回答四问：核心抽象 / 数据流动 / 关键决策点 / 边界划分
- **填不出 §2 = 不许拆**（写成闸门，而不是流程里的一个环节）
- 步骤表新增一列 **「贴着 QwenPaw 的什么」**，**不许空** ——
  填不出来 = 那一步是凭空想的 → 删掉重来（脚手架写「——（无对应）」但必须显式）
- 最危险的信号：§2.3「我们简化 / 换掉了什么」那栏**是空的** ——
  要么确实原样照搬了（少见），要么**没意识到自己正在改设计**

**同步更新**：`AGENTS.md`（L7 + §3.1.1 具体动作）、`CLAUDE.md`（兜底表 + 「五条最容易踩的」）、
harness §4.1/§2.5.2/§2.5.3/§8.2/§4.3.1/§12、`docs/进度/_模板-细粒度步骤.md` 重写、
`docs/进度/README.md`、**任务清单 24 个模块条目全部标注「L7：拆步骤前精读这一栏」**、
`feature_list.json`、`README.md`。

**下一步**
- 开工 `M01` / `a01` —— **先过 L7 闸门**（读 `model_factory.py` / `routing_chat_model.py` / `providers/`）
