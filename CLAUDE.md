# CLAUDE.md — CaiPaw-Learning（Claude Code 入口）

> **唯一真源是 `AGENTS.md`。** 改规则请改那边。
> 本文件只做两件事：① 把 `AGENTS.md` 导入进来；② 兜底放一份硬约束速查。
>
> ⚠️ 若你修改了 `AGENTS.md` 的 §1，**请同步下面这份速查**（只有这一处是重复的，故意的）。

@AGENTS.md

---

## 兜底速查：硬约束（若上面的导入没生效，以本段为准）

### ★ 学习纪律（优先级最高）

| # | 约束 | 适用 |
|---|---|---|
| **L1** | **MUST** 一次只动一个 feature（WIP=1） | 始终 |
| **L2** | **MUST NOT** 一步超过 **~300 行**、**3 个文件**，或「不能立刻跑起来看到结果」 | 始终 |
| **L3** | **MUST** 每步产出 `learning-notes/step-NNN-*.md`（新概念 + 关键代码位置 + 认知落差 + 自测结果） | 始终 |
| **L4** | **MUST** 每步结束后请用户**自己动手改一处**并观察行为变化（写进笔记） | 始终 |
| **L5** | **MUST NOT** 让看不懂的代码进仓库；看不懂就当场问，**不许攒着** | 始终 |
| **L6** | **MUST** 每重构一个模块前先读 QwenPaw 对应模块；写完对照，差异写进笔记 | 涉及 QwenPaw 时 |

### 工程纪律

> ⚠️ 「适用条件」列不能省 —— 省了就变成「所有规则所有时候都适用」，
> 规则会腐化（`harness机制_CaiPaw-Learning.md` §4.1 的 A.7）。

| # | 约束 | 适用条件 |
|---|---|---|
| E1 | **MUST** commit message 引用 feature ID（例：`feat(a03): 加第一个工具`） | 始终 |
| E2 | **MUST NOT** 提交 API key / token / 私钥 | 始终 |
| E3 | **MUST NOT** 提交模型权重 / 数据集 / 日志 / 缓存 | 始终 |
| E4 | **MUST NOT** 提交 `qwenpaw-project/` | 始终 |
| E5 | **MUST NOT** 直接抄 QwenPaw 代码；只可读、对照，实现自己写 | 涉及 QwenPaw 时 |
| E6 | **MUST** 两子系统只通过 HTTP 通信，不共享数据库、不共享代码 | 子系统 ② 建起来之后 |
| E7 | **MUST** 模型访问统一走 OpenAI 兼容协议，外部 API ↔ 自建服务只改配置 | 接模型时 |
| E8 | **MUST NOT** 直接在服务器改源码（服务器只 `git pull`） | 服务器侧 |
| E9 | **MUST NOT** 在服务器约定的公共目录之外留存运行资源 | 服务器侧 |
| E10 | **MUST** 服务器推理服务绑 `127.0.0.1`，不对公网暴露 | 起服务时 |
| **E11** | **MUST NOT** 把服务器标识信息（主机别名 / IP / 绝对路径 / GPU 型号 / 端口 / 用户名）或任何密钥写进入库文件 —— 只允许在 `docs/服务器运维手册.md`（不入库） | 始终 |

### 四条最容易踩的

1. **L2** — 你天然倾向「一次给全」。>300 行 / >3 文件 / 看不到立刻能跑的结果 → **停，拆成两步。**
2. **L6** — 不读 QwenPaw 就动手 = 大概率写偏，且学不到设计思路。
3. **拆解** — 只拆**当前**任务的细粒度步骤。不要提前把后面任务的步骤也拆出来。
4. **★ 别用真值做危险测试** — 用真 key 去验证「key 会不会被拦住」，
   验证过程本身就把 key 送进了风险位置。**假数据一样能测。**
   （踩过：`git add -f .env` 的「强加测试」把真 key 写进了 `.git/objects`。
   见 `harness机制_CaiPaw-Learning.md` §3.2.4）
