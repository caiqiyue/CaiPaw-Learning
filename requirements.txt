# ============================================================
# CaiPaw-Learning — 子系统 ①（agent-service/）运行时依赖
# ============================================================
# 对应技术栈：Python 3.11 + FastAPI + LangChain / LangGraph
# 环境：conda env `caipaw-agent`
#
# ⚠️ 这是「起步约束」，**不是锁定版本**。
#    用 `>=` 是为了让依赖能解析成功；真正的锁版本要等
#    agent-service/ 建起来、第一版跑通之后，用
#    `pip freeze > requirements.lock.txt` 生成。
#    现在写死版本号是在猜 —— 猜错了比不写更麻烦。
#
# ⚠️ 另有一份 requirements-dev.txt（开发工具），
#    装了它 init.sh 才跑得通。开发机上装那个。
#
# ⚠️ 等 agent-service/ 建起来并引入 pyproject.toml 之后，
#    **那份 pyproject.toml 成为唯一真源**，本文件届时改成指针或直接删掉。
# ============================================================

# ---- 1. 模型访问（阶段 A：M01 就会用到）----
# E7：统一走 OpenAI 兼容协议 —— 外部 API ↔ 服务器 vLLM 只改配置，不动代码。
langchain-core>=0.3
langchain-openai>=0.2
langgraph>=0.2
openai>=1.40

# ---- 2. 配置与数据校验（阶段 A：M01 就会用到）----
# 密钥从 .env 读、不进仓库（E2）；配置用 pydantic 校验，写错在启动期就报。
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# ---- 3. HTTP / 网络（工具与流式都要用）----
httpx>=0.27


# ============================================================
# 以下【阶段 B】才开始需要（M05 服务骨架起）。
# 现在取消注释装了也没用，只会拖慢环境安装 —— 到哪一步再开哪一段。
# ============================================================

# ---- 4. 服务层（阶段 B）----
# fastapi>=0.115
# uvicorn[standard]>=0.30

# ---- 5. 流式输出（阶段 B：M05 的 SSE）----
# sse-starlette>=2.1


# ============================================================
# 明确【不需要】的东西（写下来免得以后有人手贱加）
# ============================================================
# agentScope        —— 上游 QwenPaw 用的框架，正是我们要换掉的那个
# vllm              —— 跑在服务器上，是独立进程；本仓库只当它是 OpenAI 兼容端点
# torch / transformers —— 同上，本地不做推理
